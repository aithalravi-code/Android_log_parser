export const implementation_plan = {
    "phase1": {
        "title": "Unify Interaction Logic",
        "steps": [
            "Refactor `handleViewportInteraction` into distinct handlers: `handleLogLineClick`, `handleTableRowClick`, `handleCopyAction`.",
            "Implement a strict hierarchy: Copy (Ctrl+Click) > Text Selection > Row Selection.",
            "Ensure all interactive sets (Log Viewports, CCC Table, Stats Table, BTSnoop) use this unified handler."
        ]
    },
    "phase2": {
        "title": "Robust Scroll Restoration",
        "steps": [
            "Virtual Views (Logs, BTSnoop): Refine `applyFiltersAsync` to decouple rendering from scrolling. Create a `scrollToAnchor` function that runs *post-render*.",
            "Standard Tables (CCC, Stats): Implement a `selectedRowId` state tracking. Assign unique IDs (hash/timestamp) to rows. On re-render, find the row by ID and `scrollIntoView()`."
        ]
    },
    "phase3": {
        "title": "Universal granular Copy",
        "description": "Ensure every data-bearing character is copyable without breaking selection.",
        "steps": [
            "Add `copy-cell` class and `data-log-text` to ALL render functions (CCC, Device Events, etc).",
            "Update CSS to ensure `copy-cell` provides hover feedback (cursor) only when Ctrl is pressed (optional polish)."
        ]
    }
}